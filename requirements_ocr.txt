# olmOCR dependencies
# NOTE: Requires Python >= 3.11

# --- Pick ONE of the following install lines ---
# For NVIDIA GPU (recommended):
#   pip install olmocr[gpu] --extra-index-url https://download.pytorch.org/whl/cu128
# For CPU / Apple Silicon (slower, no vLLM):
#   pip install olmocr

# Core dependencies (installed automatically by olmocr)
# torch
# transformers
# Pillow
# qwen-vl-utils

# Additional required packages
accelerate
pypdf
pymupdf
